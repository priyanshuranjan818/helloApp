version: "3.9"

services:
  web:
    image: haxx616/helloappV2-web:latest
    container_name: helloapp-web
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=development
    restart: unless-stopped


